public enum TimePeriod
{
    Days,
    Weeks,
    Years,
}

public enum QueryOptions
{
    FilterByTimePeriod,
    OrderBy,
}

public enum SortField 
{ 
    SessionDate, 
    Duration 
}

public enum SortDirection
{
    ASC,
    DESC
}